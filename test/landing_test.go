package test

import (
	"testing"
)

func TestLanding(t *testing.T) {
	// t.Log(utils.MD5("amount=42&notify_url=http://example.com/notify&order_id=20220201030210321&redirect_url=http://example.com/redirectepusdt_password_xasddawqe"))
	// t.Log(order, err)
	// tmpl, _ := template.New("demo").Parse(Tempate_Line)

	// args1 := map[string]string{"lineID": "Hypermind"}

	// err := tmpl.ExecuteTemplate(os.Stdout, "T", args1)

	// t.Log("hello", err)

	// var buf bytes.Buffer

	// tpl, _ := template.ParseFiles("../templates/Template_Line")
	// fmt.Println(tpl.Name())
	// // t.Execute(w, "Hello world")

	// err := tpl.Execute(&buf, map[string]string{"lineID": "Hypermind"})

	// t.Log(buf.String(), err)

	// var buf bytes.Buffer

	// tpl, _ := template.ParseFiles("../templates/Template_Line")
	// fmt.Println(tpl.Name())
	// // t.Execute(w, "Hello world")

	// err := tpl.Execute(&buf, map[string]string{"lineID": "Hypermind"})

	// t.Log(buf.String(), err)
	// content, err := ioutil.ReadFile("../templates/Template_Meta")
	// content, err := ioutil.ReadFile("../templates/Template_Line")
	// content, err := ioutil.ReadFile("../riben/index.html")
	// if err != nil {
	// 	fmt.Println("无法读取文件")
	// 	return
	// }

	// if ok, _ := regexp.Match("<!--hot-->", []byte("hello")); ok {
	// 	fmt.Println("ok")
	// }\

	// text := "<!--hot-->hellox<!--hot-->"
	// re := regexp.MustCompile(`<!--hot-->(.+?)<!--hot-->`)
	// matches := re.FindStringSubmatch(text)
	// if len(matches) > 1 {
	//     fmt.Println(matches[1]) // 输出: hellox
	// }

	// lineReg := regexp.MustCompile(`(?s)<!--hot-->\s*(.*?)\s*<!--hot-->`) //替换掉那一个

	// lineReg := regexp.MustCompile(`(?s)<!-- Meta Pixel Code -->\s*(.*?)\s*<!-- End Meta Pixel Code -->`) //替换掉那一个

	// 	lineReg := regexp.MustCompile(`(?s)(<head>)(.*?)(</head>)`) //替换掉那一个

	// 	output := lineReg.ReplaceAllString(string(content), "${1}${2}helloxBBB${4}${5}")

	// t.Log(output)

	// ioutil.WriteFile("1.html", []byte(output), 0644)

	// text := "<!--hot-->hellox<!--hot-->"

	// matches := re.FindStringSubmatch(text)

	// matches := lineReg.FindStringSubmatch(string(content))

	// if len(matches) > 1 {
	// 	fmt.Println(matches[1]) // 输出: hellox
	// }

	// content = lineReg.ReplaceAll(content, []byte(""))

	// indexContent = string(content)

	// t.Log(indexContent)

	// ti := utils.NewTemplateInject()
	// ti.LoadIndexHtml()
	// ti.Write()
}
